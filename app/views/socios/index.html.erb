<p id="notice"><%= notice %></p>

<h1>Socios</h1>

<table>
  <thead>
    <tr>
      <th>Nome</th>
      <th>CPF</th>
      <th>Administrador</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @juridica.socios.each do |socio| %>
      <tr>
        <td><%= socio.fisica.nome %></td>
        <td><%= socio.fisica.cpf %></td>
        <td><%= socio.administrador ? 'Sim' : 'Não' %></td>   
        <td><%= link_to 'Show', fisica_path(socio.fisica) %></td>
        <td><%= link_to 'Edit', edit_juridica_socio_path(socio) %></td>
        <td><%= link_to 'Destroy', juridica_socio_path(socio), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Socio', new_juridica_socio_path(@juridica) %>
