<%= form_with(model: @socio, url: ([@juridica, @socio])) do |form| %>
  <% if @socio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(socio.errors.count, "error") %> prohibited this socio from being saved:</h2>

      <ul>
      <% @socio.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :administrador %>
    <%= form.check_box :administrador %>
  </div>

  <div class="field">
    <%= form.collection_select :fisica_id, Fisica.all.order(updated_at: :desc), :id, :nome %></br>
  </div>

  <div class="field">
    <label>Razão Social:</label>
    <input type="text" name="token" class="form-control" disabled="disabled" value="<%= @juridica.razao %>">
  </div>

  <div class="row">
  <div class="col-md-6 col-md-offset-3">
    
  </div>
</div>

  <div class="botao_texto_da_acao">
    <%= form.submit texto_da_acao, class: "btn btn-view botoes_socios"%>
  </div>
<% end %>

<%= link_to 'Voltar', juridica_path(@juridica), class: "btn btn-default" %>